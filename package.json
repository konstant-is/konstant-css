{
  "name": "@konstant/css",
  "private": "false",
  "version": "1.0.0",
  "files": [
    "styles"
  ],
  "scripts": {
    "build": "pnpm clean && tsup",
    "dev": "tsup --watch",
    "clean": "rm -rf dist",
    "prepare": "husky"
  },
  "dependencies": {
    "postcss": "^8.4.49",
    "sass": "^1.83.0"
  },
  "devDependencies": {
    "esbuild-sass-plugin": "^3.3.1",
    "husky": "^9.1.7",
    "tsup": "^8.3.5",
    "typescript": "latest"
  }
}
